import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CYCLONE — Cyclone Monitoring & Prediction System",
  description:
    "Real-time cyclone tracking, historical analysis, and AI-powered prediction platform.",
  keywords: ["cyclone", "hurricane", "typhoon", "tracking", "prediction", "weather"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
    >
      <body className="min-h-full flex flex-col bg-[#090a0d] text-[#f3f4f6]">
        {children}
      </body>
    </html>
  );
}
